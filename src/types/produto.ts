export type Produto = {
    id: number,
    name: string,
    preco: number,
    qtd: number
}

export type ProdutoSelecionado = {
    id: number,
    name: string,
    precoUni: number,
    qtdEscolhida: number
}